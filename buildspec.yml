version: 0.2

phases: 
  pre_build:
    commands: 
      - echo "Pre-build phase"
  build:
    commands:
      - echo "Building the application"
      - mvn package
  post_build:
    commands:
      - echo "Post-build phase"
      
artifacts:
  files:
    - target/*